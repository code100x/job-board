export const publicRoutes = ["/", "/verification", "/api/uploadthing"];

export const authRoutes = ["/login", "/register", "/reset", "/new-password"];

export const apiAuthPrefix = "/api/auth";

export const DEFAULT_LOGIN_REDIRECT = "/jobs";
